# Dependências diretas do Mafinho Explora
# Gerado em 2026-03-26
#
# Para recriar o ambiente local:
#   python -m venv .venv311
#   .venv311\Scripts\activate
#   python install_tts.py        # instala TTS com contorno do bug setuptools
#   pip install -r requirements.txt
#
# ATENÇÃO: a pasta .venv311 NÃO é sincronizada com a nuvem (ver .gitignore).
# Ela ocupa ~2,7 GB localmente e deve ser recriada em cada máquina nova.

# --- Geração de voz (Coqui XTTS-v2) ---
TTS==0.22.0

# --- PyTorch (CPU — não usar versão GPU a menos que necessário) ---
torch==2.5.1+cpu
torchaudio==2.5.1+cpu
--extra-index-url https://download.pytorch.org/whl/cpu

# --- Utilitários ---
numpy==1.26.4
scipy==1.17.1
requests==2.32.5
